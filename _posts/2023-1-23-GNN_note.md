---
layout: post
title: "Notes on GNN"
date: 2023-1-23 +0300
type: "blog"
---

# Graph Representation 
- adjacency matrix
- adjacency list (there seems to be two types)
  - element being a list of the neighboring nodes of a node (This one from data structure course...)
  - element being a list of the ending nodes of an edge[^GNNintro]

# Permutation invariance
## 
- input: vector in the space $X$ spanned by edges, vertices and globals $\vec{x}$
- output: $f\in Y$
- GNN: $f: X\to Y$, involving operators such as (discrete) Laplacian $L$

## Permutation
The permutation of a vector can be represented by a matrix

$$
P_{(x_1, x_2, x_3)}
= 
\begin{pmatrix}
    \delta_{x_{1},1} & \delta_{x_{2},1} & \delta_{x_{3},1} \\
    \delta_{x_{1},2} & \delta_{x_{2},2} & \delta_{x_{3},2} \\
    \delta_{x_{1},3} & \delta_{x_{2},3} & \delta_{x_{3},3}
\end{pmatrix},
$$

where $PP^T=I$ obviously holds because $\delta_{x_{k}, i}\delta_{x_{k}, j} = \delta_{x_k,x_k}\delta_{i,j} = \delta_{i,j}$.

On a graph, applying permutation to the adjacency matrix is $PAP^T$

**Permutation invariance**
- $f(\vec{x}) = f(P\vec{x})$


**Permutation equivariance**[^GNNWed]
- $Pf(\vec{x}) = f(P\vec{x})$
  > if we permute the nodes, it doesn’t matter if we do it before or after the function!
- So the dimension of the domain and codomain of $f$ should be the same?

## Smoothing
In the message-passing scheme in GNNs, a node gathers information from its neighbors and combine their features into its own to update. This process leads to neighboring nodes have similar representations. It is undesired when we are, say, to classify the nodes if all of them are somehow homogeneous.

**Quantification**[^GNNBlog]
- MAD and MADGap
- Group Distance Ratio


**To be continued**

[^GNNintro]: https://distill.pub/2021/gnn-intro/
[^GNNWed]: https://petar-v.com/talks/GNN-Wednesday.pdf
[^GNNBlog]: https://towardsdatascience.com/over-smoothing-issue-in-graph-neural-network-bddc8fbc2472